#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    new_matrix = len(matrix [])
    while (matrix):
        for row in matrix:
            x = [x]
            for col in row:
                new_matrix = x**2
        return new_matrix
